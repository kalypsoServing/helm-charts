apiVersion: v2
name: kalypso-minio
description: MinIO S3-compatible object storage for Kalypso infrastructure
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: minio
    version: "5.4.0"
    repository: "https://charts.min.io/"
    condition: minio.enabled
